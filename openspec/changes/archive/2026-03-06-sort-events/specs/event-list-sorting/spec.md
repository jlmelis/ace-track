## ADDED Requirements

### Requirement: Sort Events Newest to Oldest
The system SHALL sort the displayed list of events and tournaments such that the most recently dated entries appear at the top.

#### Scenario: Displaying multiple events
- **WHEN** the user navigates to a view displaying a list of events
- **THEN** the items in the list appear sorted in descending order based on their date (newest first)
