## 1. Data Fetching Updates

- [x] 1.1 Update the event/tournament fetching logic in `db.ts` (e.g., `getEvents` or related functions) to sort the returned array by date descending (newest first).

## 2. Verification

- [x] 2.1 Start the development server and verify the event list on the main views shows newest events at the top.
- [x] 2.2 Verify that the sorting change doesn't negatively impact offline operation.
